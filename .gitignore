/sonarqube-prometheus-exporter
*.test
*.out
.DS_Store
dist/
# ci.yml's version/release jobs install semantic-release ephemerally
# (npm install --no-save) -- never committed, but ignore it in case anyone
# runs .github/semantic-release/*.mjs locally.
node_modules/
